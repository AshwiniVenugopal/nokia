spring.datasource.url = jdbc:mysql://localhost:3306/ashwini

spring.datasource.username = root

spring.datasource.password = 261096

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update

spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect

spring.jpa.generate-ddl=true

spring.jpa.show-sql=true

spring.application.name=media-service

server.port=8080

# Enable multipart uploads

spring.servlet.multipart.enabled=true

# Threshold after which files are written to disk.

spring.servlet.multipart.file-size-threshold=900MB

# Max file size.

spring.servlet.multipart.max-file-size=900MB

# Max Request Size

spring.servlet.multipart.max-request-size=915MB

# All files uploaded through the REST API will be stored in this directory
file.upload-dir=/Users/ashani/Documents/upload
